# ⚠️ КРИТИЧНО: Виправлення помилки "backend: not found"

## Проблема

Railway намагається виконати `backend` як команду:
```
sh: 1: backend: not found
```

## ✅ Рішення 1: Встановити Root Directory (ОБОВ'ЯЗКОВО!)

**Це найважливіше!** Без цього нічого не працюватиме.

### В Railway Dashboard:

1. **Відкрити проект "ZkPersona"**
2. **Натиснути на ваш backend service**
3. **Перейти на вкладку "Settings"** (⚙️ або три крапки ⋯ → Settings)
4. **Знайти "Source" або "Root Directory" або "Working Directory"**
5. **Встановити значення:** `backend`
6. **Натиснути "Save" або "Update"**
7. **Перейти на "Deployments" → "Redeploy"**

---

## ✅ Рішення 2: Створено railway.json в корені

Якщо Root Directory не встановлено, створено `railway.json` в корені, який явно вказує на `backend`:

```json
{
  "build": {
    "buildCommand": "cd backend && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && npm start"
  }
}
```

**Але це не замінює Root Directory!** Краще встановити Root Directory = `backend`.

---

## 🔍 Як перевірити, що Root Directory встановлено:

1. Railway Dashboard → ваш проект
2. Settings → Source
3. Має бути: `backend` (не порожньо!)

---

## 📝 Покрокова інструкція:

### Крок 1: Відкрити Settings
- Railway Dashboard
- Ваш проект "ZkPersona"
- Ваш backend service
- **Settings** (⚙️)

### Крок 2: Знайти "Source" або "Root Directory"
- Прокрутити вниз до секції "Source"
- Або знайти "Root Directory"
- Або "Working Directory"

### Крок 3: Встановити значення
- Ввести: `backend`
- Натиснути **Save**

### Крок 4: Redeploy
- Перейти на "Deployments"
- Натиснути **"Redeploy"**

---

## ⚠️ Якщо все одно не працює:

1. Перевірити, що Root Directory = `backend` (не `/backend`, не `./backend`, просто `backend`)
2. Перевірити, що збережено зміни
3. Зробити redeploy
4. Перевірити логи - має бути `npm install` та `npm start`, а не `backend`

---

## ✅ Очікуваний результат:

Після встановлення Root Directory, в логах має бути:

```
✓ Detected Node.js project
✓ Running: npm install
✓ Running: npm start
```

**НЕ має бути:**
```
✖ backend: not found
```

---

## 🎯 Швидке рішення:

1. **Railway Dashboard → Settings → Root Directory → `backend`**
2. **Save**
3. **Deployments → Redeploy**

Готово! ✅
