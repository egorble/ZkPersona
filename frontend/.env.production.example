# Production Backend URL
# Replace with your production backend URL (Railway, Render, VPS, etc.)
VITE_BACKEND_URL=https://your-backend-domain.com

# Optional: Override Aleo program ID (default: zkpersona_passport_v5.aleo)
# Set to your deployed program id so claims always use your contract.
# VITE_PROGRAM_ID=your_program_id.aleo
